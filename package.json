{
  "name": "poomas",
  "private": true,
  "version": "0.1.0",
  "description": "Poomas Traveldays — Multi-tenant white-label B2B/B2C travel portal",
  "scripts": {
    "build":          "turbo run build",
    "dev":            "turbo run dev",
    "lint":           "turbo run lint",
    "typecheck":      "turbo run typecheck",
    "test":           "turbo run test",
    "db:generate":    "pnpm --filter @poomas/db drizzle-kit generate",
    "db:migrate":     "pnpm --filter @poomas/db drizzle-kit migrate",
    "db:push":        "pnpm --filter @poomas/db drizzle-kit push",
    "db:seed":        "pnpm --filter @poomas/db tsx src/seed.ts",
    "db:studio":      "pnpm --filter @poomas/db drizzle-kit studio",
    "deploy:api":     "pnpm --filter @poomas/api deploy",
    "deploy:web":     "pnpm --filter @poomas/web deploy",
    "deploy:portal":  "pnpm --filter @poomas/portal deploy",
    "deploy:admin":   "pnpm --filter @poomas/admin deploy",
    "clean":          "turbo run clean && find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "prettier": "^3.3.0",
    "eslint": "^9.12.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0"
}
